extends layouts/main
block head
  style.
    .fx3 {
      background:transparent;
    }
    h2 {line-height:3rem;}
block content
  form.form(action="login",method="POST")
    .section.cm
      h2 欢迎登录 #{basic.site}
    input(type="hidden",name="_csrf",value=csrf)
    .section.mt2.vm
      label.pt1.fx1(for="username") 用户名：
      input.bb.fx3#username(type="text",required,name="user[username]")
    .section.mt2.vm
      label.fx1(for="password") 密码：
      input.bb.fx3#password(type="password",required,name="user[password]")
    .section.mt2.vm
      input.btn.pct50w.left25.mb1.mt1(type="submit",value="登录")