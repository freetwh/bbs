	1.xhr对象post来的数据，如何获取。

	2.url里传的参数如何获取
	参数获取的三种方式：
		req.params.var	获取路径匹配 例如：/:id
		req.body.var	获取post表单里的控件值
		req.query.var	获取url里传的参数    √
	3.如何在一个路由里查询两个表的数据并发送至页面
		由于方法（find/findOne)是异步执行的，无法在上下文获取查询结果，
		（1）可在方法内，用res.locals.var = var接受查询结果
		在最后一次查询里render页面就行。
		（2）用promise接收




设想方案：（ps:做主题会上瘾的前前端~~）
	/*第二套主题方案：
纯色系。（参照ui中国）*/
/*主题可切换，固定条左边显示切换（简/酷）
将色系样式提取分离到主题样式文件，复制一份更改色调统一。
用jq为切换按钮绑定点击事件监听，点击后主题样式切换（简/酷放在主题文件伪类里）
*/